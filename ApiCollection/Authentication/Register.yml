info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: "{{ADB_baseURL}}/api/register"
  body:
    type: json
    data: |-
      {
          "firstname": "Killian",
          "lastname": "Godet",
          "email": "test258@email.com",
          "password": "Abricot2024!",
          "confirmPassword": "Abricot2024!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
