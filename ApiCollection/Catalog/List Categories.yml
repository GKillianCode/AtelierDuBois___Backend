info:
  name: List Categories
  type: http
  seq: 4

http:
  method: GET
  url: "{{ADB_baseURL}}/api/public/v1/category/all"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
