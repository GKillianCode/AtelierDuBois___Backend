info:
  name: List Products
  type: http
  seq: 1

http:
  method: GET
  url: "{{ADB_baseURL}}/api/public/v1/products?page=1"
  params:
    - name: page
      value: "1"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
