info:
  name: Check Creation Eligibility
  type: http
  seq: 2

http:
  method: GET
  url: "{{ADB_baseURL}}/api/v1/user/addresses/eligibility"
  auth:
    type: bearer
    token: "{{ADB_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
