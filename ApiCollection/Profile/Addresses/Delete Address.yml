info:
  name: Delete Address
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{ADB_baseURL}}/api/v1/user/addresses/6D39zpKj5AtKsrffOaRA6f"
  auth:
    type: bearer
    token: "{{ADB_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
