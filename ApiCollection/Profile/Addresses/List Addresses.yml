info:
  name: List Addresses
  type: http
  seq: 3

http:
  method: GET
  url: "{{ADB_baseURL}}/api/v1/user/addresses"
  auth:
    type: bearer
    token: "{{ADB_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
