info:
  name: Update Address
  type: http
  seq: 5

http:
  method: PUT
  url: "{{ADB_baseURL}}/api/v1/user/addresses/6YNjqe9bfpKHcZWTNceD1Y"
  auth:
    type: bearer
    token: "{{ADB_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
