info:
  name: Get history years
  type: http
  seq: 2

http:
  method: GET
  url: "{{ADB_baseURL}}/api/v1/shipment/history/years"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
