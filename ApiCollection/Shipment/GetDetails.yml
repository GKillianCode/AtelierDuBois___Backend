info:
  name: GetDetails
  type: http
  seq: 3

http:
  method: GET
  url: "{{ADB_baseURL}}/api/v1/shipment/history/77"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
