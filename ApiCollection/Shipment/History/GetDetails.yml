info:
  name: GetDetails
  type: http
  seq: 1

http:
  method: GET
  url: "{{ADB_baseURL}}/api/v1/shipment/history/77"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
