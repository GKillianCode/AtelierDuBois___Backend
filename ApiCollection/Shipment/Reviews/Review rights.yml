info:
  name: Review rights
  type: http
  seq: 4

http:
  method: GET
  url: "{{ADB_baseURL}}/api/v1/shipment/SHP-2605-3GDPQVMM/review-rights"
  auth:
    type: bearer
    token: "{{ADB_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

